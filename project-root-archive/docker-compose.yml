services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webassembly-ide-web
    ports:
      - "3000:3000"
    restart: unless-stopped

networks:
  default:
    name: webassembly-ide-network
