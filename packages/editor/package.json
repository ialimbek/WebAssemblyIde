{
  "name": "@webassembly-ide/editor",
  "version": "0.1.0",
  "private": true,
  "description": "Monaco Editor integration for WebAssemblyIde",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "node --test src/**/*.test.ts",
    "lint": "echo 'Lint not configured yet'"
  },
  "dependencies": {
    "@webassembly-ide/shared": "workspace:*",
    "@webassembly-ide/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
