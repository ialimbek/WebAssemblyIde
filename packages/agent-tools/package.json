{
  "name": "@webassembly-ide/agent-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Tool Registry for Agent Runtime",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "node --test src/**/*.test.ts",
    "lint": "echo 'Lint not configured yet'"
  },
  "dependencies": {
    "@webassembly-ide/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
