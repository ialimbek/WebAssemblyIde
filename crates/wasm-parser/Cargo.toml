[package]
name = "wasm-parser"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
tree-sitter = "0.22"
# Additional parser dependencies will be added
