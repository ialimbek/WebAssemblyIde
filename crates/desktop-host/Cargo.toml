[package]
name = "desktop-host"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dependencies.tauri]
version = "2"
optional = true

[features]
default = []
tauri = ["dep:tauri"]
